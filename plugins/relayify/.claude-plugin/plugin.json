{
  "name": "relayify",
  "description": "Write front-end code (React/TSX or plain HTML) in Relay Financial's design language. Bundles 48 component specs, design tokens, a curated style guide, and an MCP server that any coding agent can query. The consumer skill auto-activates on Relay project work.",
  "version": "0.2.1",
  "author": {
    "name": "Relay Financial"
  },
  "mcpServers": {
    "relayify": {
      "command": "${CLAUDE_PLUGIN_ROOT}/mcp-server/find-node.sh",
      "args": ["${CLAUDE_PLUGIN_ROOT}/mcp-server/dist/index.js"],
      "env": {
        "RELAY_REGISTRY_ROOT": "${CLAUDE_PLUGIN_ROOT}/registry"
      }
    }
  }
}
