{
  "name": "relayify",
  "description": "Write front-end code (React/TSX or plain HTML) in Relay Financial design language. Bundles 70 component specs (48 page-level patterns + 22 Telegraph primitives), design tokens, a curated style guide, and an MCP server that any coding agent can query. The consumer skill auto-activates on Relay project work.",
  "version": "0.3.3",
  "author": {
    "name": "Relay Financial"
  },
  "mcpServers": {
    "relayify": {
      "command": "${CLAUDE_PLUGIN_ROOT}/mcp-server/find-node.sh",
      "args": [
        "${CLAUDE_PLUGIN_ROOT}/mcp-server/dist/index.js"
      ],
      "env": {
        "RELAY_REGISTRY_ROOT": "${CLAUDE_PLUGIN_ROOT}/registry"
      }
    }
  }
}
